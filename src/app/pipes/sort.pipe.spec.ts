/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SortPipe } from './sort-and-filter.pipe';

describe('Pipe: Sorte', () => {
  it('create an instance', () => {
    let pipe = new SortPipe();
    expect(pipe).toBeTruthy();
  });
});
